<!DOCTYPE html>

<%- include('../partials/head'); %>

<html lang="en">
  <body class="container">
    <%- include('../partials/header'); %>

    <main>
      <div class="jumbotron">
        <h1>Node com EJS</h1>
        <p>Vamos aprender a trabalhar com arquivos EJS</p>
        <p><%= subtitle %></p>
      </div>
      <ul>
        <% qualidades.forEach(function(i){ %>
        <li><strong><%= i.title %></strong><%= i.message %></li>

        <% }) %>
      </ul>
    </main>

    <%- include('../partials/footer'); %>
  </body>
</html>
